openapi: 3.0.3
info:
  title: Aircup API
  version: 0.1.0
  description: |
    Aircup API for browsing and streaming personal files across devices.

servers:
  - url: http://localhost:8080

tags:
  - name: health
  - name: files

paths:
  /api/files:
    get:
      operationId: listFiles
      tags: [files]
      summary: List files and directories under a relative path
      parameters:
        - in: query
          name: path
          required: true
          description: Relative path from configured root, usually starting with /
          schema:
            type: string
            example: /
      responses:
        "200":
          description: Directory listing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileListResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden path
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Path not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/files/content:
    get:
      operationId: getFileContent
      tags: [files]
      summary: Stream or serve a file inline
      parameters:
        - in: query
          name: path
          required: true
          description: Relative path from configured root
          schema:
            type: string
            example: /Videos/demo.mp4
      responses:
        "200":
          description: File content streamed inline when supported
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
            video/mp4:
              schema:
                type: string
                format: binary
            audio/mpeg:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden path
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: File not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    ErrorResponse:
      type: object
      required: [message]
      properties:
        message:
          type: string
          example: invalid request

    FileListResponse:
      type: object
      required: [path, entries]
      properties:
        path:
          type: string
          example: /Videos
        entries:
          type: array
          items:
            $ref: "#/components/schemas/FileEntry"
    FileEntry:
      type: object
      required: [name, path, is_dir, size, modified_at]
      properties:
        name:
          type: string
          example: demo.mp4
        path:
          type: string
          example: /Videos/demo.mp4
        is_dir:
          type: boolean
          example: false
        size:
          type: integer
          format: int64
          example: 1048576
        modified_at:
          type: string
          format: date-time
          example: 2026-04-21T10:30:00Z
